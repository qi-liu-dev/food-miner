Designer category files go here. Name each file with the backend asset ID, for example:

- category-korean.svg (or category_korean.svg)
- category-japanese.svg
- category-pizza.svg

The React app discovers these files automatically with `import.meta.glob`.
